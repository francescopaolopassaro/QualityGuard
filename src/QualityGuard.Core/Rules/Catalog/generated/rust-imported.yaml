# Catalogue entries written in one pass, one per defect this language still lacks a check for.
# Every entry is documentation and carries no detection, so none of them reports anything: a
# detection is written by hand, against the shape the defect actually has, and measured before
# the entry stops being planned.

- key: QG-RS-SML-0011
  name: Wildcard imports should not be used
  languages: [rust]
  category: SML
  severity: critical
  effort: 5min
  status: planned
  generated: true
  tags: [pitfall, clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-SML-0012
  name: Unnecessary mathematical comparisons should not be made
  languages: [rust]
  category: SML
  severity: critical
  effort: 15min
  status: planned
  generated: true
  tags: [clippy, suspicious]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-SML-0013
  name: Erasing mathematical operations should not be performed
  languages: [rust]
  category: SML
  severity: major
  effort: 15min
  status: planned
  generated: true
  tags: [clumsy, clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-SML-0014
  name: Boolean expressions should not be gratuitous
  languages: [rust]
  category: SML
  severity: major
  effort: 10min
  status: planned
  generated: true
  tags: [cwe, suspicious, redundant, clippy]
  summary: Code that no longer takes part in anything is still here.
  why: |
    It has to be read, understood and kept compiling by everyone who passes through, and it gives
    nothing back. Worse, it suggests a purpose that no longer exists.
  fix: |
    Remove it: the history keeps it if it is ever needed again.

- key: QG-RS-SML-0015
  name: Array elements should be separated by commas
  languages: [rust]
  category: SML
  severity: major
  effort: 2min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-SML-0016
  name: Mathematical constants should not be hardcoded
  languages: [rust]
  category: SML
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy, clumsy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-SML-0017
  name: Inline vector literals should be preferred to chains of insertions
  languages: [rust]
  category: SML
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: An API is used where a better one exists.
  why: |
    The older form usually costs something the newer one does not, and it is on its way out: a
    conversion, an allocation, a case it handles wrongly.
  fix: |
    Use the current API, and keep the old one only behind a documented compatibility layer.

- key: QG-RS-SML-0018
  name: Pointer arithmetic should not be performed on zero-sized types
  languages: [rust]
  category: SML
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-SML-0019
  name: Invisible Unicode characters should not be used
  languages: [rust]
  category: SML
  severity: critical
  effort: 10min
  status: planned
  generated: true
  tags: [pitfall, clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-SML-0020
  name: Cognitive Complexity of functions should not be too high
  languages: [rust]
  category: SML
  severity: critical
  effort: 15min
  status: planned
  generated: true
  tags: [brain-overload]
  summary: A piece of code has grown past what can be held in mind at once.
  why: |
    The size is the defect: every change to it costs more than it should and carries a risk that has
    nothing to do with the change itself.
  fix: |
    Split it along the responsibilities it has accumulated.

- key: QG-RS-SML-0021
  name: "if ... else if" constructs should end with "else" clauses
  languages: [rust]
  category: SML
  severity: critical
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-SML-0022
  name: Field init shorthand should be used
  languages: [rust]
  category: SML
  severity: minor
  effort: 1min
  status: planned
  generated: true
  tags: [clippy]
  summary: An API is used where a better one exists.
  why: |
    The older form usually costs something the newer one does not, and it is on its way out: a
    conversion, an allocation, a case it handles wrongly.
  fix: |
    Use the current API, and keep the old one only behind a documented compatibility layer.

- key: QG-RS-SML-0023
  name: Redundant casts should be avoided
  languages: [rust]
  category: SML
  severity: minor
  effort: 1min
  status: planned
  generated: true
  tags: [redundant, clippy]
  summary: Code that no longer takes part in anything is still here.
  why: |
    It has to be read, understood and kept compiling by everyone who passes through, and it gives
    nothing back. Worse, it suggests a purpose that no longer exists.
  fix: |
    Remove it: the history keeps it if it is ever needed again.

- key: QG-RS-SML-0024
  name: Standard outputs should not be used directly to log anything
  languages: [rust]
  category: SML
  severity: major
  effort: 10min
  status: planned
  generated: true
  tags: [bad-practice, clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-SML-0025
  name: Closures should be replaced with function pointers
  languages: [rust]
  category: SML
  severity: minor
  effort: 2min
  status: planned
  generated: true
  tags: [clippy]
  summary: An API is used where a better one exists.
  why: |
    The older form usually costs something the newer one does not, and it is on its way out: a
    conversion, an allocation, a case it handles wrongly.
  fix: |
    Use the current API, and keep the old one only behind a documented compatibility layer.

- key: QG-RS-SML-0026
  name: Underscores should be used to make large numbers readable
  languages: [rust]
  category: SML
  severity: minor
  effort: 5min
  status: planned
  generated: true
  tags: [convention, clippy]
  summary: A name does not follow the convention the rest of the code uses.
  why: |
    Conventions let a reader infer what something is from its shape alone. A deviation costs a
    lookup every time, and makes searches and automated refactoring less reliable.
  fix: |
    Rename it to match the convention used around it.

- key: QG-RS-SML-0027
  name: Shared code in all branches should be extracted
  languages: [rust]
  category: SML
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The control flow does not do what it reads as doing.
  why: |
    A branch that cannot be taken, a loop that cannot end, a condition that is always the same: the
    code states an intent the machine does not carry out, and review reads the intent.
  fix: |
    Make the flow say what it does, and remove what cannot happen.

- key: QG-RS-SML-0028
  name: Match expression conditions should not have boolean type
  languages: [rust]
  category: SML
  severity: minor
  effort: 10min
  status: planned
  generated: true
  tags: [clippy]
  summary: The control flow does not do what it reads as doing.
  why: |
    A branch that cannot be taken, a loop that cannot end, a condition that is always the same: the
    code states an intent the machine does not carry out, and review reads the intent.
  fix: |
    Make the flow say what it does, and remove what cannot happen.

- key: QG-RS-SML-0029
  name: Rust source files should not have syntax errors
  languages: [rust]
  category: SML
  severity: major
  effort: 30min
  status: planned
  generated: true
  tags: [suspicious]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0008
  name: Null pointers should not be passed to functions expecting non-null arguments
  languages: [rust]
  category: BUG
  severity: critical
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: A value that can be absent is used as though it always exists.
  why: |
    The path that produces the absence is usually the rare one, so this survives testing and arrives
    in production as a failure with no obvious cause.
  fix: |
    Handle the absent case where it arises, or make the type say the value is always there.

- key: QG-RS-BUG-0009
  name: Getters should access the expected fields
  languages: [rust]
  category: BUG
  severity: critical
  effort: 5min
  status: planned
  generated: true
  tags: [pitfall, clippy]
  summary: Something is reachable by callers who should not reach it.
  why: |
    Access granted this broadly is never noticed afterwards: nothing fails, and the surface stays
    open for as long as the resource exists.
  fix: |
    Grant the access to the identities that need it and to nothing else.

- key: QG-RS-BUG-0010
  name: Accessing an array element should not trigger a panic
  languages: [rust]
  category: BUG
  severity: critical
  effort: 10min
  status: planned
  generated: true
  tags: [clippy]
  summary: Something is reachable by callers who should not reach it.
  why: |
    Access granted this broadly is never noticed afterwards: nothing fails, and the surface stays
    open for as long as the resource exists.
  fix: |
    Grant the access to the identities that need it and to nothing else.

- key: QG-RS-BUG-0011
  name: Await should be used for awaitable returns in async blocks and functions
  languages: [rust]
  category: BUG
  severity: critical
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: Shared state is reached without the coordination that makes it safe.
  why: |
    It works every time it is tested and fails under load, on a machine with more cores, once. That
    is the hardest kind of defect to reproduce and the easiest to write.
  fix: |
    Guard the shared state, or give each caller its own copy.

- key: QG-RS-BUG-0012
  name: Manual PartialOrd implementation should be avoided when Ord is derived
  languages: [rust]
  category: BUG
  severity: critical
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0013
  name: Avoid manual PartialEq implementation with a derived Hash
  languages: [rust]
  category: BUG
  severity: critical
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: Cryptography is used in a way that does not give the protection it appears to.
  why: |
    The code still runs and the data still looks protected, which is what makes this expensive to
    find: nothing fails until somebody who should not be able to read the data reads it.
  fix: |
    Use the current recommended algorithm and parameters, and let the platform choose what it can.

- key: QG-RS-BUG-0014
  name: C-like enums should not have unportable variants
  languages: [rust]
  category: BUG
  severity: critical
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0015
  name: Case mismatches in pattern arms of match expressions should be avoided
  languages: [rust]
  category: BUG
  severity: blocker
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The control flow does not do what it reads as doing.
  why: |
    A branch that cannot be taken, a loop that cannot end, a condition that is always the same: the
    code states an intent the machine does not carry out, and review reads the intent.
  fix: |
    Make the flow say what it does, and remove what cannot happen.

- key: QG-RS-BUG-0016
  name: Raw pointers should not be casted to slices with differently sized elements
  languages: [rust]
  category: BUG
  severity: blocker
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0017
  name: Variables should be swapped using `std::mem::swap`
  languages: [rust]
  category: BUG
  severity: blocker
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0018
  name: Incompatible bit masks should not be used in comparisons
  languages: [rust]
  category: BUG
  severity: blocker
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0019
  name: Calls to `std::mem::transmute` should not be evaluated eagerly
  languages: [rust]
  category: BUG
  severity: critical
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0020
  name: The `#[inline]` attribute should not be used on trait methods without implementation
  languages: [rust]
  category: BUG
  severity: critical
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0021
  name: Calls to `step_by` that always panic should not be made
  languages: [rust]
  category: BUG
  severity: blocker
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The control flow does not do what it reads as doing.
  why: |
    A branch that cannot be taken, a loop that cannot end, a condition that is always the same: the
    code states an intent the machine does not carry out, and review reads the intent.
  fix: |
    Make the flow say what it does, and remove what cannot happen.

- key: QG-RS-BUG-0022
  name: Comparisons with overlapping ranges that are always false should not be made
  languages: [rust]
  category: BUG
  severity: critical
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The control flow does not do what it reads as doing.
  why: |
    A branch that cannot be taken, a loop that cannot end, a condition that is always the same: the
    code states an intent the machine does not carry out, and review reads the intent.
  fix: |
    Make the flow say what it does, and remove what cannot happen.

- key: QG-RS-BUG-0023
  name: Conflicting `Borrow` and `Hash` implementations should be avoided
  languages: [rust]
  category: BUG
  severity: critical
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: Cryptography is used in a way that does not give the protection it appears to.
  why: |
    The code still runs and the data still looks protected, which is what makes this expensive to
    find: nothing fails until somebody who should not be able to read the data reads it.
  fix: |
    Use the current recommended algorithm and parameters, and let the platform choose what it can.

- key: QG-RS-BUG-0024
  name: Variables should not be self-assigned
  languages: [rust]
  category: BUG
  severity: major
  effort: 3min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0025
  name: Loops with at most one iteration should be refactored
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [confusing, bad-practice, clippy]
  summary: The control flow does not do what it reads as doing.
  why: |
    A branch that cannot be taken, a loop that cannot end, a condition that is always the same: the
    code states an intent the machine does not carry out, and review reads the intent.
  fix: |
    Make the flow say what it does, and remove what cannot happen.

- key: QG-RS-BUG-0026
  name: Related "if/else if" statements should not have the same condition
  languages: [rust]
  category: BUG
  severity: major
  effort: 10min
  status: planned
  generated: true
  tags: [clippy, unused, pitfall]
  summary: The control flow does not do what it reads as doing.
  why: |
    A branch that cannot be taken, a loop that cannot end, a condition that is always the same: the
    code states an intent the machine does not carry out, and review reads the intent.
  fix: |
    Make the flow say what it does, and remove what cannot happen.

- key: QG-RS-BUG-0027
  name: "while" loop counters should not have floating type
  languages: [rust]
  category: BUG
  severity: minor
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The control flow does not do what it reads as doing.
  why: |
    A branch that cannot be taken, a loop that cannot end, a condition that is always the same: the
    code states an intent the machine does not carry out, and review reads the intent.
  fix: |
    Make the flow say what it does, and remove what cannot happen.

- key: QG-RS-BUG-0028
  name: Clamping values with `cmp::min` and `cmp::max` should use correct ranges
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0029
  name: Avoid resizing a vector to zero using `vec.resize(0, value)`
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0030
  name: Avoid transmutes that can never be correct
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0031
  name: Immutable variables should not be used in while loop conditions
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The control flow does not do what it reads as doing.
  why: |
    A branch that cannot be taken, a loop that cannot end, a condition that is always the same: the
    code states an intent the machine does not carry out, and review reads the intent.
  fix: |
    Make the flow say what it does, and remove what cannot happen.

- key: QG-RS-BUG-0032
  name: Lint attributes should not be used on crate imports
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0033
  name: I/O buffers should be processed entirely
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0034
  name: Collections should not be transmuted to different types
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0035
  name: Unit values should not be hashed
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: Cryptography is used in a way that does not give the protection it appears to.
  why: |
    The code still runs and the data still looks protected, which is what makes this expensive to
    find: nothing fails until somebody who should not be able to read the data reads it.
  fix: |
    Use the current recommended algorithm and parameters, and let the platform choose what it can.

- key: QG-RS-BUG-0036
  name: Unit values should not be compared
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0037
  name: `MaybeUninit::uninit().assume_init()` should not be used
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0038
  name: Null pointers should not be transmuted
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: A value that can be absent is used as though it always exists.
  why: |
    The path that produces the absence is usually the rare one, so this survives testing and arrives
    in production as a failure with no obvious cause.
  fix: |
    Handle the absent case where it arises, or make the type say the value is always there.

- key: QG-RS-BUG-0039
  name: Null function pointers should not be created through `transmute`
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: A value that can be absent is used as though it always exists.
  why: |
    The path that produces the absence is usually the rare one, so this survives testing and arrives
    in production as a failure with no obvious cause.
  fix: |
    Handle the absent case where it arises, or make the type say the value is always there.

- key: QG-RS-BUG-0040
  name: `splitn` should not be used with a limit of 0 or 1
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0041
  name: `size_of::<T>` should not be used to count elements of type `T`
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0042
  name: Reversed ranges and slices should not be empty
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: Code that no longer takes part in anything is still here.
  why: |
    It has to be read, understood and kept compiling by everyone who passes through, and it gives
    nothing back. Worse, it suggests a purpose that no longer exists.
  fix: |
    Remove it: the history keeps it if it is ever needed again.

- key: QG-RS-BUG-0043
  name: Redundant comparisons should be removed
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: Code that no longer takes part in anything is still here.
  why: |
    It has to be read, understood and kept compiling by everyone who passes through, and it gives
    nothing back. Worse, it suggests a purpose that no longer exists.
  fix: |
    Remove it: the history keeps it if it is ever needed again.

- key: QG-RS-BUG-0044
  name: Formatting trait implementations should not be recursive
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0045
  name: Lines read from the standard input should be trimmed
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: A piece of code has grown past what can be held in mind at once.
  why: |
    The size is the defect: every change to it costs more than it should and carries a risk that has
    nothing to do with the change itself.
  fix: |
    Split it along the responsibilities it has accumulated.

- key: QG-RS-BUG-0046
  name: `unwrap()` should only be used when there is a value to unwrap
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0047
  name: `checked_add` and `overflowing_add` should be used to prevent overflows
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: An API is used where a better one exists.
  why: |
    The older form usually costs something the newer one does not, and it is on its way out: a
    conversion, an allocation, a case it handles wrongly.
  fix: |
    Use the current API, and keep the old one only behind a documented compatibility layer.

- key: QG-RS-BUG-0048
  name: `env!` should be preferred over `option_env!`
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: An API is used where a better one exists.
  why: |
    The older form usually costs something the newer one does not, and it is on its way out: a
    conversion, an allocation, a case it handles wrongly.
  fix: |
    Use the current API, and keep the old one only behind a documented compatibility layer.

- key: QG-RS-BUG-0049
  name: Functions expecting raw pointer arguments should be marked as unsafe
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0050
  name: File open options should be consistent
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0051
  name: Unix file permissions should be set with octal values
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: Something is reachable by callers who should not reach it.
  why: |
    Access granted this broadly is never noticed afterwards: nothing fails, and the surface stays
    open for as long as the resource exists.
  fix: |
    Grant the access to the identities that need it and to nothing else.

- key: QG-RS-BUG-0052
  name: Synchronization locks should not be dropped immediately after acquisition
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: Shared state is reached without the coordination that makes it safe.
  why: |
    It works every time it is tested and fails under load, on a machine with more cores, once. That
    is the hardest kind of defect to reproduce and the easiest to write.
  fix: |
    Guard the shared state, or give each caller its own copy.

- key: QG-RS-BUG-0053
  name: Remainder operations with `1` or `-1` should be avoided
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0054
  name: Functions should not return mutable references from immutable parameters
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: A piece of code has grown past what can be held in mind at once.
  why: |
    The size is the defect: every change to it costs more than it should and carries a risk that has
    nothing to do with the change itself.
  fix: |
    Split it along the responsibilities it has accumulated.

- key: QG-RS-BUG-0055
  name: The return value of `next` should not be looped over
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The control flow does not do what it reads as doing.
  why: |
    A branch that cannot be taken, a loop that cannot end, a condition that is always the same: the
    code states an intent the machine does not carry out, and review reads the intent.
  fix: |
    Make the flow say what it does, and remove what cannot happen.

- key: QG-RS-BUG-0056
  name: `skip(0)` should not be used on iterators
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0057
  name: Inherent shadowing definitions of to_string should be avoided
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0058
  name: `set_len` should not be called on uninitialized vectors
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0059
  name: Serde `visit_str` method should be implemented when `visit_string` is implemented
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0060
  name: `mem::uninitialized` and `mem::zeroed()` should not be used to replace values
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: An API is used where a better one exists.
  why: |
    The older form usually costs something the newer one does not, and it is on its way out: a
    conversion, an allocation, a case it handles wrongly.
  fix: |
    Use the current API, and keep the old one only behind a documented compatibility layer.

- key: QG-RS-BUG-0061
  name: `saturating_sub` should be used to avoid subtraction underflow
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: An API is used where a better one exists.
  why: |
    The older form usually costs something the newer one does not, and it is on its way out: a
    conversion, an allocation, a case it handles wrongly.
  fix: |
    Use the current API, and keep the old one only behind a documented compatibility layer.

- key: QG-RS-BUG-0062
  name: Infinite iterators should be finished off with a terminating operation
  languages: [rust]
  category: BUG
  severity: major
  effort: 5min
  status: planned
  generated: true
  tags: [clippy]
  summary: The code does not behave the way it reads.
  why: |
    Written this way it works by accident or fails on ordinary input, and the mistake survives
    review because the intent reads correctly.
  fix: |
    Make the intended behaviour explicit, and cover the case that used to fail with a test.

- key: QG-RS-BUG-0063
  name: Non-empty statements should change control flow or have at least one side-effect
  languages: [rust]
  category: BUG
  severity: major
  effort: 10min
  status: planned
  generated: true
  tags: [cwe, unused, clippy]
  summary: Code that no longer takes part in anything is still here.
  why: |
    It has to be read, understood and kept compiling by everyone who passes through, and it gives
    nothing back. Worse, it suggests a purpose that no longer exists.
  fix: |
    Remove it: the history keeps it if it is ever needed again.
